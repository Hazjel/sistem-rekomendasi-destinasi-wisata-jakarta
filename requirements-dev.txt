-r requirements.txt
jupyter
jupytext

# Riset perbandingan metode CBF (NB 05 sec.7-8: TF-IDF vs sentence embedding).
# Opsional — sistem produksi (web + optimasi) hanya butuh requirements.txt.
torch
transformers
huggingface_hub
